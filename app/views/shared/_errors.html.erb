
<h2>Errors</h2>
<% if @userNew.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@userNew.errors.count, "error") %>.
    </div>
    <ul>
    <% @userNew.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>